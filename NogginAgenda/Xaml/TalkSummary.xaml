<?xml version="1.0" encoding="UTF-8"?>
<ContentView
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="NogginAgenda.TalkSummary"
	xmlns:converters="clr-namespace:NogginAgenda.Xaml.Converters;assembly=NogginAgenda"
	Padding="0,10">

	<ContentPage.Resources>
    	<ResourceDictionary>
        	<converters:UriConverter x:Key="uriConverter"/>
    	</ResourceDictionary>
	</ContentPage.Resources>

	<ContentView.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="50" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<!--<Image Source="{Binding Speaker.PictureUrl}" WidthRequest="50" HeightRequest="50" VerticalOptions="Start" />-->
			<Image WidthRequest="50" HeightRequest="50" VerticalOptions="Start">
    			<Image.Source>
      				<UriImageSource Uri="{Binding Speaker.PictureUrl, Converter={StaticResource uriConverter}}" CacheValidity="5" />
    			</Image.Source>
  			</Image>

			<StackLayout Grid.Column="1">
				<Label Text="{Binding Title}" Font="Bold,Small" />
				<Label Text="{Binding Speaker.Name}" Font="Micro" />
			</StackLayout>

		</Grid>
	</ContentView.Content>
</ContentView>